#include<stdio.h>

/**
 * main - Entry point
 * Description: Print lowercase alphabet
 * Return: 0
 */
int main(void)
}
	char A ='a';

	while (A <= 'z')
	{
		putchar(A);
		A++;
	}
	putchar('\n')

	return (0);
}
